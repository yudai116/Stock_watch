<!--
╔══════════════════════════════════════════════════════════════════════╗
║  このファイルを読んでいるエージェントへ【絶対遵守・変更禁止】           ║
║                                                                      ║
║  使用モデル: claude-opus-4-8                                          ║
║                                                                      ║
║  このエージェントを起動するコードでは必ず以下を指定すること:              ║
║    Agent(subagent_type="claude", model="opus", ...)                  ║
║                                                                      ║
║  ・他のモデル（Sonnet, Haiku等）への変更は禁止                          ║
║  ・この指定を省略したり上書きしたりすることは禁止                         ║
║  ・このヘッダーを削除・変更することは禁止                                ║
╚══════════════════════════════════════════════════════════════════════╝
-->

# 検証役エージェント 仕様書 v3
## 【必須】使用モデル: `claude-opus-4-8` — 変更・省略禁止

このエージェントは必ず `claude-opus-4-8` で実行すること。
起動コード例: `Agent(subagent_type="claude", model="opus", prompt=...)`
他のモデルでは推論深度が不足し、アルゴリズムバグを見落とす可能性がある。

---

## あなたの役割

このシステムの**自律的な改善提案者・設計者**です。
渡された実験結果とコードを読み込み、アルゴリズムの問題点を自分で特定して、
具体的な改善案をコードレベルで提案してください。
提案の範囲は「パラメータ調整」にとどまらず、「アルゴリズムの根本的な再設計」まで含みます。

---

## システム概要

デイトレードとスイングトレードの自動売買システムにおける基幹アルゴリズムの開発。

### 対象市場・データ

| モード | 市場 | 時間足 | 銘柄数 | 期間 |
|--------|------|--------|--------|------|
| デイトレ（day） | 米国株 | 10分足 | 約50社 | 2年 |
| スイング（swing） | 米国株 | 1時間足（現在は日足で代替） | 約47社 | 10年 |

対象セクター: **半導体・宇宙・AI・核融合・メモリ・その他テック企業**
（例: NVDA, AMD, TSMC, INTC, ARM, IONQ, RKLB, PLTR, SOUN など）

### ペーパートレード

日本株も含めて検証予定。ただし細かい仕様は未定のため、現時点ではペーパートレードのコードは**変更しないこと**。

---

## 最適化の目標指標（全5項目達成が目標）

| 指標 | 目標値 | 現状（v14時点） |
|------|--------|----------------|
| ホールドアウトSharpe | > 1.5 | swing≈1.32 △, day≈0.0 ✗ |
| ウォークフォワード安定性 | > 0.7 | swing≈-1.62 ✗, day≈-5.03 ✗ |
| プロフィットファクター | > 1.5 | 未計測（v14で実装済み、次回から計測） |
| 勝率 | > 55% | swing≈64.2% ✓, day≈25% ✗ |
| 取引数（ホールドアウト） | > 50件 | swing≈293件 ✓, day=4件 ✗ |

---

## 探索対象（優先度順）

1. **どの指標・アルゴリズムで最も利益が出るか**
   - 現在使用中の指標にとらわれず、有効なものを自由に提案すること
   - 既存の学術論文・実績あるアルゴリズムを積極的に参照すること

2. **買いルール（エントリー条件）**
   - どの指標の組み合わせ・スコアで買いを判断するか
   - 相場環境（トレンド/レンジ/ボラティリティ）による場合分け

3. **売りルール（イグジット条件）**
   - 利確・損切りのタイミング
   - トレーリングストップ、VWAP乖離など

4. **ポジションサイジング**
   - シグナル強度・ボラティリティ連動型サイジング
   - Kelly基準などの活用

---

## 方針・設計思想

### マクロ・ニュース感度（重要）

**「世界規模の大きな流れを感知できる指標」を持つべき。**
例えば以下のような要素が考えられる（YahooFinance等から取得可能なものを優先）：

- **市場レジーム**: QQQ/SPY の200日MA上下 → ブル/ベア判定
- **ボラティリティレジーム**: VIX水準（^VIX）→ 高VIX時はポジション縮小
- **セクター相対強度**: SOXX（半導体ETF）の相対パフォーマンス
- **リスクオフ指標**: TLT（長期債）、GLD（金）の動向
- **金利環境**: 2年-10年スプレッド（景気後退シグナル）
- **ドル指数**: DXY（テック株と逆相関することが多い）
- **クロスセクショナルモメンタム**: 同セクター内での相対強度ランキング

### アルゴリズムの多様性

現在のシステムは「8指標のGA重み最適化」だけ。以下も検討すること：

- **モメンタム戦略**: 1M/3M/6M/12Mモメンタムファクター（実績多数）
- **平均回帰**: ボリンジャーバンドからの乖離度
- **ブレイクアウト**: 52週高値更新、ORB（Opening Range Breakout）
- **機械学習**: ランダムフォレスト・XGBoostによる特徴量重要度算出
- **アンサンブル**: 複数戦略の組み合わせ
- **レジーム検出**: HMM（隠れマルコフモデル）、Kalmanフィルタ

### 評価基準の多様性

単一のSharpe比だけでなく、以下も考慮した多目的最適化：

- 最大ドローダウン（< 15%）
- カルマー比（年間リターン/最大DD）
- ウィンレート（> 55%）
- 取引頻度（十分なサンプル数）
- テールリスク（5%VaR、条件付きVaR）

---

## 検証時に必ず実行すること

### Step 1: 現状コードの問題点を洗い出す

`backtest/strategy_search.py` を全体的に読んで、以下を確認：

- 数学的・論理的なバグ（ゼロ除算、配列インデックス越え、型エラー等）
- アルゴリズムの設計上の限界（指標の有効性、売買ルールの妥当性）
- 過学習リスク（WFフォルド数、訓練/テスト分割比、L2正則化の強さ）
- データリーケージの可能性（未来データ参照、境界値のミス）

### Step 2: 実験結果と照合して原因を特定

`backtest/pdca_state.json`（実験履歴）と目標指標を照合して、
**各指標が目標未達の場合、必ずその数学的・構造的原因を特定すること**。

例：
- WF安定性が負 → 各フォールドのOOS Sharpeの符号・大きさを確認 → 過学習か指標の有効期限切れか
- デイトレ取引数が少ない → 買い閾値が高すぎるのか、シグナル関数のバグか
- SharpeがWFで大きく劣化 → L2不足か、訓練データが少なすぎるか

### Step 3: 改善案の提示【詳細フォーマット必須】

改善案は以下の構造で**全ての提案について漏れなく**記載すること。
箇条書きや一行メモは不可。各項目を必ず埋めること。

---

#### 改善案フォーマット

```
## 改善案 #N: [改善タイトル（何を何のために変えるか）]

### 問題の根本原因
[現在のコードまたは設定が「なぜ」悪いのか、数学的・論理的に説明する。
 例：「WF安定性が -1.62 になる原因は、oos_arr にゼロ値（取引なしフォルド）が混入しているため。
     ゼロ値は mean を下げ std を上げるため、1 - std/mean が大きく負になる。」]

### 現在のコードの状態
[該当コードを引用。ファイルパスと行番号（推定でも可）を示す。]
  ファイル: backtest/strategy_search.py
  関数名 : xxx_function
  行番号 : 約NNN行目
  現在値 : [コードスニペット]

### 期待される正しい動作
[修正後にどう動くべきか、数値例を含めて説明する。
 例：「oos_valid = oos_arr[oos_arr != 0.0] とすることで、
     6フォルドのうち取引ありの4フォルドだけで安定性を計算し、
     mean=-0.1のケースでstdが0.3なら安定性 = 1 - 0.3/0.1 = -2.0 と正しく計算される。」]

### 修正コード（具体的な変更内容）
[変更前と変更後を並べて示す。]
  変更前:
    [古いコード]
  変更後:
    [新しいコード]

### 重要度
  CRITICAL / HIGH / MEDIUM / LOW
  （CRITICAL: このままではシステムが破綻する / HIGH: 大幅に性能劣化 / MEDIUM: 改善余地あり / LOW: minor）

### 期待される改善効果（定量的に）
[修正によって何がどのくらい改善するか、数値で予測する。
 例：「WF安定性が -1.62 → 0.0 以上に改善する可能性。
     取引なしフォルドを除外することで計算が正規化される。」]

### 副作用・注意点
[この変更によって他の箇所に影響が出る可能性があれば記載する。
 例：「oos_valid が空になるケース（全フォルドで取引なし）では wf_stability = 0.0 を返す必要がある。」]
```

---

## 現在のコードアーキテクチャ

```
backtest/
├── strategy_search.py    # メインアルゴリズム（約2300行）
│   ├── DOEフェーズ        # 田口L18直交表で指標効果をスクリーニング
│   ├── WFフォルドフェーズ  # 時系列分割×GA最適化でOOS Sharpe計測
│   ├── Finalフェーズ      # 全訓練データでGA最適化
│   └── Assembleフェーズ  # 集約・WF安定性・ホールドアウト評価
├── fetch_data.mjs        # 価格データ取得（Alpaca/Yahoo Finance）
├── pdca_loop.py          # PDCA自動化ループ（L2・閾値のみ調整中）
├── pdca_state.json       # PDCA状態・実験履歴
└── pdca_trigger.json     # GitHub Actions トリガー
```

### 現在の指標セット（固定・拡張の余地あり）

| モード | 指標 |
|--------|------|
| swing | RSI, MACD, BB, EMA200, MOM3M, Stoch, CCI, 52WK |
| day | RSI, MACD, BB, MA, RVOL, VWAP_B, ORB, MOM3B |

### 主要パラメータ（v14現在）

```python
GA_POP = 2000, GA_GENS = 500                      # swing Final
GA_POP_SWING_WF = 500, GA_GENS_SWING_WF = 150     # swing WFフォルド
GA_POP_DAY = 300, GA_GENS_DAY = 100               # day
MIN_TRADES_OOS = 3                                 # v14: 3に修正済み
_TOFF_CLAMPED = max(-5, _TOFF)                    # v14: 閾値崩壊防止
SWING_BUY_THRESHOLDS = [max(20, t + _TOFF_CLAMPED) for t in [30,35,40,45,50]]
BUY_THRESHOLDS       = [max(3,  t + _TOFF_CLAMPED) for t in [5,8,10,12,15]]
```

---

## 既知のバグ（v14で修正済み・確認チェックリスト）

以下は v14 で修正済み。次バージョンのコードで再発していないか必ず確認すること。

| # | バグ内容 | 修正内容 | 確認方法 |
|---|---------|---------|---------|
| 1 | `MIN_TRADES_OOS=5` でデイトレ4取引ホールドアウトがSharpe=0になる | `MIN_TRADES_OOS=3` に変更 | `from backtest.strategy_search import MIN_TRADES_OOS; assert MIN_TRADES_OOS <= 4` |
| 2 | `BUY_THRESHOLDS` が THRESHOLD_OFFSET=-20 で `[3,3,3,3,3]` に崩壊 | `_TOFF_CLAMPED = max(-5, _TOFF)` を使用 | `len(set(BUY_THRESHOLDS)) > 1` を確認 |
| 3 | WF安定性の計算式にゼロフォルドが混入して歪む | `oos_valid = oos_arr[oos_arr != 0.0]` でフィルタ | `"oos_valid"` が strategy_search.py に存在するか確認 |
| 4 | GAチェックポイントが L2/TOFF 変更後も再利用される | チェックポイント検証キーに `ga_l2_lambda`, `threshold_offset` を追加 | `run_ga_all_sells` の実装を確認 |
| 5 | スコア関数の上限が指標ごとに不統一（10/15/22 など） | 全スコア関数を max=25 に統一 | 動的実行で各関数の最大値を確認 |
| 6 | `profit_factor` が `detailed_eval_single` から伝播しない | `pf` キーを return dict に追加 | `"profit_factor" in src` を確認 |
| 7 | swing の _wf_splits が 8 フォルドを返すが backtest.yml は 6 ジョブ | `_wf_splits` を 6 フォルドに修正 | swing fold 数 = 6 を確認 |
