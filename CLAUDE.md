# Stock_watch — Claude Code ガイド

## リポジトリ概要
米国テック株50銘柄を対象とした3層バックテストシステム。
- **レジーム層**: HMM（GaussianHMM）でマクロ相場環境を分類
- **戦略層**: GA（遺伝的アルゴリズム）で指標ウェイトを最適化
- **リスク層**: ボラティリティターゲットでポジションサイズ調整
- **モード**: SWING（1時間足 × 10年）/ DAY（10分足 × 10年）
- **検証**: ウォークフォワード4フォールド、80/20 train/test

## 絶対ルール（変更不可）
- 先読みバイアス禁止。時刻tの特徴量はtまでのデータしか使わない。
- WFのアウトオブサンプル結果のみを成績として報告。インサンプル最適化の数字を成績として出さない。
- 全バックテストに現実的な取引コスト（手数料・スリッページ・スプレッド）を入れる。
- パラメータ・状態数はデータで決める（AIC/BIC・WF）。ハードコード禁止。
- データソースにYahoo Financeを主用しない。価格乖離 > 0.5% はブロッキングエラー。
- 呼び出し元監査：関数シグネチャを変えたら全呼び出し元（特にpipeline.py）を確認する。

## セキュリティ制約
- 実際のコード変更・pushはユーザーが別途承認するまで自動でcommit/pushしない（調査・検証・ローカルファイル編集は自動でよい）。
- GitHubへのpush、本番デプロイ（Railway/Vercel）、GitHub Actions本番実行は必ずユーザー確認後。

## 4エージェント全自動運用プロトコル

ユーザーが `/loop` を起動したとき、以下の4エージェントが順番に自動起動して1ユニットの改善サイクルを回す。

```
architect → executor → checker → reviewer → architect → ...
```

### 各エージェントの役割

| エージェント | モデル | 役割 |
|---|---|---|
| architect | opus | 設計を問い直し、次の改善案を1つ決定してexecutorに渡す |
| executor | sonnet | 改善案を実装してcheckerに渡す |
| checker | sonnet | コード配線の正しさを機械的に検証。PASS/REJECTを出す |
| reviewer | opus | バックテスト結果の統計的妥当性を検証。architectに申し送り |

### ループの起動方法
```
/loop
```

### HANDOFFノート
各エージェントは作業完了時に以下を簡潔に書く:
- 何を・なぜ・どこを変えたか
- 波及先（呼び出し元・設定ファイル等）
- 次のエージェントへの申し送り事項

### checkerがREJECTした場合
executorが指摘箇所を修正し、再度checkerに渡す。checkerがPASSするまでreviewerには進まない。

### reviewerがREJECTした場合
executorに差し戻し、根本原因（先読みバイアス等）を修正する。

### ループの停止条件
- ユーザーが手動で停止
- reviewer が連続3回 REJECT（設計の根本見直しが必要なためarchitectがユーザーに確認を求める）
